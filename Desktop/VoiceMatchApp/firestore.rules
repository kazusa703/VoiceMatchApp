rules_version = '2';

// Firestoreのルール設定
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ログインしているユーザーのみ読み書きを許可
      allow read, write: if request.auth != null;
    }
  }
}